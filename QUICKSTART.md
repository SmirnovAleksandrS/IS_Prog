# Glitch Controller - Quick Start Guide

## ✅ Что установлено

Проект успешно создан со следующей структурой:

```
glitch_controller/
├── config.yaml              # Конфигурация по умолчанию
├── README.md                # Подробная документация (RU)
├── requirements.txt         # Зависимости Python
├── .gitignore              # Git ignore правила
├── ub/                     # Ядро фреймворка (Управляющий блок)
│   ├── __init__.py         # Публичный API
│   ├── model.py            # Модели данных (Pydantic v2)
│   ├── protocol.py         # Протокол UART (framing, CRC32)
│   ├── serial_link.py      # PySerial wrapper
│   ├── strategy.py         # Grid/Random + stубы (Bayes, Bandit)
│   ├── observe.py          # Классификатор результатов
│   ├── storage.py          # JSONL + SQLite/CSV экспорт
│   ├── viz.py              # Визуализация (heatmap, timeline)
│   ├── orchestrator.py     # FSM оркестратор
│   └── cli.py              # CLI (русский интерфейс)
├── experiments/            # Рабочая зона для экспериментов
│   ├── __init__.py         # Convenience imports
│   ├── test_imports.py     # Тест импортов
│   └── example_manual_trial.py  # Пример использования API
└── runs/                   # Выходные данные (создаётся автоматически)
```

## 🚀 Быстрый старт

### 1. Активация виртуального окружения

```powershell
.venv\Scripts\activate
```

### 2. Настройка config.yaml

Отредактируйте `config.yaml`:
- Измените `serial.port` на ваш COM-порт (например, `COM5`)
- При необходимости настройте параметры атаки в `campaign.strategy.params`

### 3. Запуск кампании

```powershell
python -m ub.cli run --config config.yaml
```

### 4. Генерация отчётов

```powershell
python -m ub.cli report --config config.yaml
```

## 📋 Реализованные функции

### ✅ Базовый функционал (работает)

- **Триггеры**: GPIO_LEVEL (rising/falling edge)
- **Атаки**: CLOCK_GLITCH в режиме COMPRESS
- **Стратегии**: Grid Search, Random Search
- **Протокол**: Полный framing с SOF, TYPE, LEN, PAYLOAD, CRC32
- **Логирование**: JSONL + экспорт в SQLite/CSV
- **Визуализация**: Тепловые карты, временные диаграммы
- **CLI**: Команды run, resume, report (все на русском)

### 🚧 Заглушки (для будущей реализации)

- **Атаки**: POWER_GLITCH, EXTRA_EDGE, HF_MUX, PHASE_SWAP
- **Триггеры**: UART_EVENT
- **Стратегии**: Bayesian Optimization, Bandit, Window Hunter
- **Классификация**: ML-based classifier

## 🔧 Примеры использования

### Пример 1: Запуск Grid Search

```yaml
# В config.yaml
campaign:
  strategy:
    name: "grid"
    params:
      tg_ns: [100, 80, 64, 50, 40, 32, 24, 20, 16]
      delay_ns: {start: 0, stop: 5000, step: 50}
      repeats_per_point: 3
```

```powershell
python -m ub.cli run --config config.yaml
```

### Пример 2: Random Search

```yaml
# В config.yaml
campaign:
  strategy:
    name: "random"
    params:
      tg_ns: [100, 80, 64, 50, 40, 32]
      delay_ns: {start: 0, stop: 5000, step: 1}
      seed: 42
```

### Пример 3: Программный доступ

```python
# experiments/my_experiment.py
import sys
sys.path.insert(0, 'C:\\projects\\IS_Prog')

from ub import Orchestrator
import yaml

with open('config.yaml') as f:
    config = yaml.safe_load(f)

orchestrator = Orchestrator(config)
orchestrator.run()
```

## 📊 Выходные файлы

После запуска кампании в папке `runs/<run_name>/`:

```
runs/avr_password_bypass_baseline/
├── events.jsonl           # Полный лог всех событий
├── events.csv             # CSV экспорт для анализа
├── results.sqlite         # SQLite база (опционально)
└── viz/                   # Визуализации
    ├── heatmap_success_rate.png
    └── timeline.png
```

## 🎯 Следующие шаги

1. **Подключите стенд** к COM-порту
2. **Проверьте config.yaml** (особенно `serial.port`)
3. **Запустите тестовую кампанию** с малым `max_trials` (например, 50)
4. **Проверьте результаты** в папке `runs/`
5. **Сгенерируйте визуализации** командой `report`

## 📚 Дополнительная информация

- Полная спецификация: `Spec.md`
- Документация: `README.md`
- Примеры: папка `experiments/`

## ⚠️ Важно

Это **исследовательский проект**! Код намеренно "падает громко" при ошибках 
для упрощения отладки. Не используйте в продакшене.

---

**Версия**: 0.1.0  
**Статус**: MVP готов к тестированию  
**Дата**: 2025-10-29
